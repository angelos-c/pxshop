Drop one editorial/hero image per brand here, named exactly:

- `eventuri.jpg`
- `black-mamba.jpg`
- `zrp.jpg`

Recommended: portrait orientation, roughly 4:5 (e.g. 1200×1500px), JPG or PNG, under ~500KB. The image is cropped to fill the tile (`object-cover`), so pick something that still reads well cropped tight on mobile.

Adding a brand later? Add its image here as `<brand-slug>.jpg` and pass `heroImage="/brand-hero/<brand-slug>.jpg"` to `<BrandShowcase>` in `src/app/page.tsx`.
